interface:
  display_name: "Local Audio Transcription"
  short_description: "Transcribe local audio to Markdown"
  default_prompt: "Use $transcribe-local-audio to transcribe local audio files into a consolidated Markdown file."

policy:
  allow_implicit_invocation: true
