# Superpowers: subagent-driven-development

Use this command only when explicitly invoked as a prompt command.

Load and follow the workflow described in:

`C:\Users\imale\.codex\vendor_imports\superpowers\skills\subagent-driven-development\SKILL.md`

User request / arguments:

`$ARGUMENTS`

Expected behavior:

- Execute an existing implementation plan using subagents where the current environment supports them.
- Split independent tasks across bounded subagent scopes.
- Keep write sets separate whenever possible.
- Use TDD discipline for implementation tasks.
- Review and reconcile subagent results in the main thread before reporting completion.
