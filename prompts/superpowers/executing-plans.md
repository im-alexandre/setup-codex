# Superpowers: executing-plans

Use this command only when explicitly invoked as a prompt command.

Load and follow the workflow described in:

`C:\Users\imale\.codex\vendor_imports\superpowers\skills\executing-plans\SKILL.md`

User request / arguments:

`$ARGUMENTS`

Expected behavior:

- Load the referenced plan.
- Review it critically before executing.
- Execute tasks in dependency order.
- Run the validations specified by the plan.
- Stop only for a real blocker, repeated verification failure, or missing critical context.
