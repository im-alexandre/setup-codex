name = "worker"
description = "Default implementation subagent."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"

developer_instructions = """
Use this agent for scoped implementation tasks.
Keep changes small.
Return changed files and validation performed.
"""

[features]
apps = false
